{
  "name": "search-homepage",
  "scripts": {
    "dev": "bunx concurrently -n tailwind,server -c green,blue \"bun dev:tailwind\" \"bun dev:server\"",
    "dev:server": "vite --port 3000",
    "dev:tailwind": "bunx tailwind -i ./src/index.css -o ./src/index.generated.css --watch",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bun run build && bunx netlify-cli deploy --prod --dir=dist"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.15",
    "vite": "^5.4.11"
  }
}
